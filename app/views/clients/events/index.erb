<h1>Events for Client <%= @client.identifier %></h1>

<% @uniq_client_events.each do |event| %>
  <h3><a href="<%= "/sources/#{@client.identifier}/events/#{event.name}" %>"><%= event.name %></a></h3>
<% end %>
